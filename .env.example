# KDE Connect Configuration
# The name or ID of the default device to send commands to
# KDE_DEFAULT_DEVICE=MyPhone

# Optional: Custom path to KDE Connect installation directory (containing kdeconnect-cli.exe)
# KDECONNECT_PATH="C:\Program Files\KDE Connect\bin"
