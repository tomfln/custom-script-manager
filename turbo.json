{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "templates/**", "*.ts", "*.json", "*.rs", "Cargo.toml"],
      "outputs": []
    }
  }
}
