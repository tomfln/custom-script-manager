import { buildSubmodule } from '@csm/core'

await buildSubmodule(async () => {
  // TODO: Implement build logic
  // await $`cd src && make`;
  // return "src/some-bin";
  throw new Error('Not implemented')
})
